[general]
ui = TTY.TTYUI
accounts = joelburget_at_gmail, joel_at_khanacademy
pythonfile=~/.mutt/offlineimap.py
fsync = False

[Account joelburget_at_gmail]
localrepository = joelburget_at_gmail_local
remoterepository = joelburget_at_gmail_remote
status_backend = sqlite
postsynchook = notmuch new

[Account joel_at_khanacademy]
localrepository = joel_at_khanacademy_local
remoterepository = joel_at_khanacademy_remote
status_backend = sqlite
postsynchook = notmuch new

[Repository joelburget_at_gmail_local]
type = Maildir
localfolders = ~/.mail/joelburget_at_gmail
nametrans = lambda folder: {'drafts':  '[Gmail]/Drafts',
                            'sent':    '[Gmail]/Sent Mail',
                            'flagged': '[Gmail]/Starred',
                            'trash':   '[Gmail]/Trash',
                            'archive': '[Gmail]/All Mail',
                            }.get(folder, folder)

[Repository joel_at_khanacademy_local]
type = Maildir
localfolders = ~/.mail/joel_at_khanacademy
nametrans = lambda folder: {'drafts':  '[Gmail]/Drafts',
                            'sent':    '[Gmail]/Sent Mail',
                            'flagged': '[Gmail]/Starred',
                            'trash':   '[Gmail]/Trash',
                            'archive': '[Gmail]/All Mail',
                            }.get(folder, folder)

[Repository joelburget_at_gmail_remote]
maxconnections = 1
type = Gmail
remoteuser = joelburget@gmail.com
remotepasseval = get_keychain_pass(account="joelburget@gmail.com", server="imap.gmail.com")
realdelete = no
nametrans = lambda folder: {'[Gmail]/Drafts':    'drafts',
                            '[Gmail]/Sent Mail': 'sent',
                            '[Gmail]/Starred':   'flagged',
                            '[Gmail]/Trash':     'trash',
                            '[Gmail]/All Mail':  'archive',
                            }.get(folder, folder)
folderfilter = lambda folder: folder not in ['[Gmail]/Trash',
                                             '[Gmail]/Important',
                                             '[Gmail]/Spam',
                                             ]

[Repository joel_at_khanacademy_remote]
maxconnections = 1
type = Gmail
remoteuser = joel@khanacademy.org
remotepasseval = get_keychain_pass(account="joel@khanacademy.org", server="imap.gmail.com")
realdelete = no
nametrans = lambda folder: {'[Gmail]/Drafts':    'drafts',
                            '[Gmail]/Sent Mail': 'sent',
                            '[Gmail]/Starred':   'flagged',
                            '[Gmail]/Trash':     'trash',
                            '[Gmail]/All Mail':  'archive',
                            }.get(folder, folder)
folderfilter = lambda folder: folder not in ['[Gmail]/Trash',
                                             '[Gmail]/Important',
                                             '[Gmail]/Spam',
                                             ]
